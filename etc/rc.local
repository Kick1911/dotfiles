# Default rc.local for void; add your custom commands here.
#
# This is run by runit in stage 2 before the services are executed
# (see /etc/runit/2).

# Network setup
ip link set enp2s0 up
ip link set wlp0s29u1u2u3 up

ip addr add 192.168.0.11/24 dev wlp0s29u1u2u3
ip addr add 10.0.0.11/24 dev enp2s0

ip route replace dev wlp0s29u1u2u3 via 192.168.0.1 to default
